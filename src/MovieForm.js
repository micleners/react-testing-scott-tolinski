import React, { Component } from 'react';

export default class MovieForm extends Component {
  render() {
    return (
      <form>
        <input type="text" />
        <button type="button">
Submit
        </button>
      </form>
    );
  }
}
